
import { CommonModule } from '@angular/common'; import { ChangeDetectorRef, Component, OnInit } from '@angular/core'; import { listAudits } from '../api/audit'; import type { AuditLog } from '../types/domain'; import { formatDate } from '../utils/format';
@Component({ selector: 'app-audit-page', standalone: true, imports: [CommonModule], template: `<main class="workspace"><header class="page-header"><div><p class="eyebrow">治理与追踪</p><h1>操作审计</h1><p>记录操作者、请求 ID、实体和不可逆状态变化。</p></div></header><div *ngIf="error" class="alert">{{ error }}</div><section class="audit-list"><article *ngFor="let log of logs"><time>{{ formatDate(log.createdAt) }}</time><strong>{{ log.actor }} · {{ log.action }}</strong><span>{{ log.entityType }} #{{ log.entityId }}</span><code>{{ log.beforeState || '-' }} → {{ log.afterState || '-' }}</code><small>{{ log.requestId }}</small></article></section></main>` })
export class AuditPage implements OnInit { logs: AuditLog[] = []; error = ''; readonly formatDate = formatDate; constructor(private readonly changeDetector: ChangeDetectorRef) {} async ngOnInit() { try { const result = await listAudits(); this.logs = result.data; } catch (reason) { this.error = String(reason); } finally { this.changeDetector.detectChanges(); } } }
